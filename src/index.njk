---
layout: "layout.njk"
title: "Chronology of Events"
---

<h1>{{ title }}</h1>
<p>This page presents a factual, chronological record of events based on the available evidence.</p>

<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Event Description</th>
      <th>Related People</th>
      <th>Related Evidence</th>
    </tr>
  </thead>
  <tbody>
  {%- for event in chronology | reverse -%}
    <tr id="{{ event.date }}">
      <td>{{ event.date }}</td>
      <td>{{ event.description }}</td>
      <td>
        {%- for slug in event.people.split(',') -%}
          {%- set person = people | findBySlug(slug.trim()) -%}
          {%- if person -%}
            <a href="/people/{{ person.slug }}/">{{ person.name }}</a>{% if not loop.last %}, {% endif %}
          {%- else -%}
            <span class="tag is-danger">{{ slug.trim() }}</span>
          {%- endif -%}
        {%- endfor -%}
      </td>
      <td>
        {%- for slug in event.evidence.split(',') -%}
          {%- set evidence_item = evidence | findBySlug(slug.trim()) -%}
          {%- if evidence_item -%}
            <a href="/evidence/{{ evidence_item.slug }}/">{{ evidence_item.title }}</a>{% if not loop.last %}, {% endif %}
          {%- else -%}
            <span class="tag is-danger">{{ slug.trim() }}</span>
          {%- endif -%}
        {%- endfor -%}
      </td>
    </tr>
  {%- endfor -%}
  </tbody>
</table>
